---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { fetchProjectsWithOrchestrations } from '@/lib/orchestration';
import type { ProjectWithOrchestration } from '@/lib/orchestration';

export async function getStaticPaths() {
  const projects = await fetchProjectsWithOrchestrations();

  return projects.map((project) => ({
    params: { slug: project.name },
    props: { project },
  }));
}

interface Props {
  project: ProjectWithOrchestration;
}

const { project } = Astro.props;
const hasOrchestration = !!project.orchestration;
const hasArchitecture = !!project.architecture;

const formattedDate = project.updatedAt.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<BaseLayout title={project.name} description={project.description}>
  <article class="hub">
    <a href="/workflow" class="back reveal">
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
        <path d="M9 3L5 7l4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
      </svg>
      Workflow
    </a>

    <header class="hero reveal">
      <h1>{project.name}</h1>
      <p class="lead">{project.description}</p>
      <div class="meta">
        {project.language && <span class="badge">{project.language}</span>}
        <span class="date">Updated {formattedDate}</span>
        <a href={project.url} target="_blank" rel="noopener noreferrer" class="gh-link">
          View on GitHub &rarr;
        </a>
      </div>
    </header>

    {hasOrchestration && project.orchestration && (
      <section class="section reveal reveal-delay-1">
        <h2>Agent Pipeline</h2>
        <p class="section-lead">{project.orchestration.description}</p>

        <div class="pipeline">
          {project.orchestration.agents.map((agent, i) => (
            <div class="pipeline-item">
              <div class="pipeline-marker">
                <span class="index">{String(i + 1).padStart(2, '0')}</span>
              </div>
              <div class="pipeline-body">
                <h3>{agent.title}</h3>
                <p>{agent.description}</p>
                {agent.outputs.length > 0 && (
                  <div class="tags">
                    {agent.outputs.map((o) => (
                      <span class="tag">{o}</span>
                    ))}
                  </div>
                )}
              </div>
            </div>
          ))}
        </div>

        <div class="graph-placeholder reveal">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="5" cy="12" r="3" />
            <circle cx="19" cy="6" r="3" />
            <circle cx="19" cy="18" r="3" />
            <path d="M8 11l8-4M8 13l8 4" />
          </svg>
          <span>Interactive workflow graph coming soon</span>
        </div>
      </section>
    )}

    {hasArchitecture && project.architecture && (
      <section class="section reveal reveal-delay-2">
        <h2>System Architecture</h2>
        <p class="section-lead">{project.architecture.description}</p>

        <div class="arch-list">
          {project.architecture.components.map((comp) => (
            <div class="arch-item">
              <div class="arch-header">
                <h3>{comp.title}</h3>
                <span class="tier">{comp.tier}</span>
              </div>
              <p class="arch-desc">{comp.description}</p>
              <span class="tech">{comp.technology}</span>
              {comp.subcomponents && comp.subcomponents.length > 0 && (
                <div class="subs">
                  {comp.subcomponents.map((sub) => (
                    <div class="sub">
                      <span class="sub-name">{sub.name}</span>
                      <span class="sub-sep"> — </span>
                      <span class="sub-detail">{sub.detail}</span>
                    </div>
                  ))}
                </div>
              )}
            </div>
          ))}
        </div>

        <div class="graph-placeholder reveal">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="3" width="7" height="7" rx="1.5" />
            <rect x="14" y="3" width="7" height="7" rx="1.5" />
            <rect x="8.5" y="14" width="7" height="7" rx="1.5" />
            <path d="M10 10l-1.5 4M14 10l1.5 4" />
          </svg>
          <span>Interactive architecture graph coming soon</span>
        </div>
      </section>
    )}
  </article>
</BaseLayout>

<style>
  .hub {
    max-width: 860px;
  }

  .back {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--muted);
    margin-bottom: 64px;
    transition: color 0.15s;
  }

  .back:hover { color: var(--ink); }

  .hero {
    margin-bottom: 96px;
    padding-bottom: 64px;
    border-bottom: 1px solid var(--rule);
  }

  .hero h1 {
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 400;
    margin-top: 0;
    margin-bottom: 16px;
    line-height: 1.05;
  }

  .lead {
    font-size: 1.1rem;
    font-weight: 400;
    color: var(--ink-3);
    max-width: 55ch;
    line-height: 1.7;
    margin-bottom: 32px;
  }

  .meta {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    align-items: center;
  }

  .badge {
    font-family: var(--font-mono);
    font-weight: 500;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--muted);
    background-color: var(--paper-2);
    padding: 6px 14px;
    border-radius: 100px;
    border: 1px solid var(--rule);
  }

  .date {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--muted);
  }

  .gh-link {
    font-size: 0.875rem;
    font-weight: 500;
    padding: 6px 16px;
    border-radius: 100px;
    border: 1px solid var(--rule);
    color: var(--muted);
    transition: color 0.15s, border-color 0.15s;
  }

  .gh-link:hover {
    color: var(--ink);
    border-color: var(--ink-3);
  }

  .section {
    margin-bottom: 96px;
  }

  .section h2 {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 600;
    margin-top: 0;
    margin-bottom: 8px;
  }

  .section-lead {
    color: var(--ink-3);
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 64px;
    padding-left: 16px;
    border-left: 3px solid var(--rule);
  }

  .pipeline {
    display: flex;
    flex-direction: column;
    position: relative;
    margin-bottom: 64px;
  }

  .pipeline::before {
    content: '';
    position: absolute;
    left: 19px;
    top: 28px;
    bottom: 28px;
    width: 1px;
    background: var(--rule);
  }

  .pipeline-item {
    display: flex;
    gap: 32px;
    padding: 16px 0;
  }

  .pipeline-marker {
    flex-shrink: 0;
    width: 40px;
    display: flex;
    justify-content: center;
    padding-top: 2px;
  }

  .index {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--muted);
    background: var(--paper);
    position: relative;
    z-index: 1;
    padding: 2px 0;
  }

  .pipeline-body {
    flex: 1;
    min-width: 0;
  }

  .pipeline-body h3 {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0 0 4px;
  }

  .pipeline-body p {
    color: var(--ink-3);
    font-size: 0.9rem;
    font-weight: 400;
    line-height: 1.6;
    margin: 0 0 8px;
  }

  .tags {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }

  .tag {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    padding: 3px 10px;
    border-radius: 100px;
    background: var(--paper-2);
    color: var(--muted);
    border: 1px solid var(--rule);
  }

  .arch-list {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-bottom: 64px;
  }

  .arch-item {
    background: var(--paper-2);
    border-radius: 8px;
    padding: 16px 32px;
  }

  .arch-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 6px;
  }

  .arch-header h3 {
    font-size: 1.05rem;
    font-weight: 600;
    margin: 0;
  }

  .tier {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--muted);
  }

  .arch-desc {
    color: var(--ink-3);
    font-size: 0.9rem;
    font-weight: 400;
    line-height: 1.6;
    margin: 0 0 4px;
  }

  .tech {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--muted);
  }

  .subs {
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px solid var(--rule);
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .sub {
    font-size: 0.85rem;
    font-weight: 400;
    line-height: 1.5;
    color: var(--muted);
  }

  .sub-name {
    font-weight: 600;
    color: var(--ink-3);
  }

  .graph-placeholder {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    height: 120px;
    border: 2px dashed var(--rule);
    border-radius: 8px;
    color: var(--muted);
    font-size: 0.85rem;
    font-weight: 400;
  }

  @media (max-width: 640px) {
    .hub { max-width: 100%; }
    .pipeline-marker { width: 30px; }
    .pipeline::before { left: 14px; }
    .pipeline-item { gap: 16px; }
  }
</style>
